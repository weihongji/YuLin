意见征求
	1. 界面文字描述
	2. 界面菜单组织方式
	3. 报表文件名字格式（名字包含日期还是放到日期文件夹）
	4. 报表内容格式（日期、数字）

报表放到日期文件夹里

解除系统的时间限制


每次发布前测试报表结果

Import SF
Import 业务状况表(各支行 )

神木/府谷 -> table function:
	SELECT Id FROM Org WHERE Name LIKE '%神木%' OR Name LIKE '%府谷%' OR Name LIKE '%神府%'

没有Import文件夹的情况下导入五级分类报错。STR：1. 导入基础数据；2. 删除Import文件夹；3. 导入五级分类